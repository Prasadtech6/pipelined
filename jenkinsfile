node{
    stage('Checkout'){
        properties([pipelineTriggers([githubPush()])])
         checkout(scm)
	 }
	 stage('build'){
	 echo "build is success"
	 }
	 stage('deploy'){
	 echo " test and deploy "
}
}
